apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: ac
  labels:
    app: api
spec:
  type: LoadBalancer
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
